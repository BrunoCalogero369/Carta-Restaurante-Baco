import MenuSection from '../components/MenuSection';
import productData from '../data/productData';

function Entradas() {
  return <MenuSection title="Entradas" products={productData} />;
}

export default Entradas;
